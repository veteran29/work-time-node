<h1>Hello {{name}}!</h1>
<script>
    var userSocket = io('/users');
    userSocket.on('connect', function() {
        userSocket.emit('work-start', { username: '{{name}}', taskId: '{{taskId}}_taskId' })
        console.log('work-start');
    });
</script>